using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamageSpells : Spells
{
    #region PROPERTIES

    private float m_Damage = 0.0f;

    #endregion

    public DamageSpells(string _Name, int _ManaCost, Sprite _Icon) : base(_Name, _ManaCost, _Icon)
    {

    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
